export enum SupplyField {
  CREATED_DATE = "CREATED_DATE",
  STATUS = "STATUS",
  DEPARTMENT = "DEPARTMENT",
  AMOUNT = "AMOUNT",
  CREATOR = "CREATOR",
  SOURCE = "SOURCE"
}

export enum Action {
  FILTER = "FILTER",
  SORT = "SORT"
}

export enum SortOption {
  ASC = "ASC",
  DESC = "DESC"
}
